export enum SelectElementOptions {
  DEFAULT_CATEGORY_OPTION = "Category",
  DEFAULT_AREA_OPTION = "Area",
}

export enum LocalStorageItems {
  RECIPES = "recipes",
}

export enum FirebaseCollections {
  USER_COLLECTION = "users",
  DAILY_RECIPES_COLLECTION = "dailySuggestions",
}

export enum FirebaseDocs {
  DEFAULT_DOC = "documentNumber001",
}
